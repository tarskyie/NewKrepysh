<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NewKrepysh.WinUI.Views.AssetsToolBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewKrepysh.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="80">

    <Grid>
        <Border Margin="12"
            Padding="12"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="12">
            <ScrollView HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Hidden">
                <StackPanel Orientation="Horizontal"
                        Spacing="8"
                        VerticalAlignment="Center">

                    <Button Style="{StaticResource AccentButtonStyle}"
                            Command="{x:Bind ViewModel.AddAssetCommand}">
                        <TextBlock>Add an asset</TextBlock>
                    </Button>

                    <Button>
                        <TextBlock>Remove selected asset</TextBlock>
                    </Button>
                
                </StackPanel>
            </ScrollView>
        </Border>
    </Grid>
</UserControl>
