<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NewKrepysh.WinUI.Views.MainToolBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewKrepysh.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="80">

    <ScrollView HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Hidden">
        <StackPanel Orientation="Horizontal">
            <Button VerticalAlignment="Stretch" 
                    Command="{Binding NewPageCommand}"
                    Content="New page" />
            <Button VerticalAlignment="Stretch" 
                    Command="{Binding RemoveSelectedPageCommand}"
                    Content="Remove selected page" />
        </StackPanel>
    </ScrollView>
</UserControl>
